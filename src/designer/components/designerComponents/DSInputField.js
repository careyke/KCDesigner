/**
 * 设计时单行文本
 */

import React from 'react';
import {InputField} from '../../../baseComponent';

export default class DSInputField extends React.Component{
    constructor(props){
        super(props);
    }

    render(){
        return(
            <InputField />
        )
    }
}